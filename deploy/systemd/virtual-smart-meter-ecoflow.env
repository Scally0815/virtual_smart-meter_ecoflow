HTTP_PORT=80
DATA_PROVIDER=mqtt
MQTT_HOST=localhost
MQTT_STATE_TOPIC=virtual-smart-meter/og/measurement
# Set this explicitly on multi-homed systems to the service's LAN address.
# MDNS_ADDRESS=192.168.1.50
# Restrict multicast to an interface name or IPv4 interface address when needed.
# MDNS_INTERFACE=eth0
